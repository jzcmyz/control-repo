---
version: 4
datadir: "hieradata"
hierarchy:
  - name: "Nodes"
    backend: yaml
    path: "nodes/%{trusted.certname}"
  - name: "%{facts.os.family}"
    backend: yaml
  - name: "common"
    backend: yaml
