---
version: 4
datadir: "hieradata"
hierarchy:
  - name: "Nodes"
    backend: yaml
    path: "nodes/%{trusted.certname}"
  - name: "%{::osfamily}"
    backend: yaml
  - name: "common"
    backend: yaml
